# file /etc/systemd/system/wakelock.service

[Unit]
Description=Lock the screen on resume from suspend

[Service]
User=najee89b
Type=forking
Environment=DISPLAY=:0
ExecStart=/usr/bin/i3lock -f -e -c 000000

[Install]
WantedBy=sleep.target
WantedBy=suspend.target
